framework:
    default_locale: 'en'
    translator:
        default_path: '%kernel.project_dir%/translations'
        fallbacks:
            - 'en' # Langue de secours en cas de traduction manquante
            - 'fr' # Langue de secours en cas de traduction manquante
            - 'ar' # Langue de secours en cas de traduction manquante
    

#        providers:
#            crowdin:
#                dsn: '%env(CROWDIN_DSN)%'
#            loco:
#                dsn: '%env(LOCO_DSN)%'
#            lokalise:
#                dsn: '%env(LOKALISE_DSN)%'

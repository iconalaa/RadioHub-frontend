<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Prescription</title>
    <style>
        /* Add your CSS styles for the PDF here */
    </style>
</head>
<body>
    <h1>Prescription</h1>
    <p>Prescription ID: {{ prescription.id }}</p>
    <p>Content: {{ prescription.contenu }}</p>
    <p>Date: {{ prescription.date ? prescription.date|date('Y-m-d') : 'N/A' }}</p>
    {% if prescription.signatureFilename %}
        <img src="data:image/png;base64,{{ imageData }}" alt="Signature">
    {% else %}
        <p>No signature available</p>
    {% endif %}
</body>
</html>

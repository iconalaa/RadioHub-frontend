<form method="post" action="{{ path('app_gratification_delete', {'id': gratification.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gratification.id) }}">
    <button class="btn">Delete</button>
</form>

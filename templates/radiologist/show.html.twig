<!DOCTYPE html>

<title>Radiologist</title>

{% block body %}
    <h1>Radiologist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ radiologist.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ radiologist.username }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ radiologist.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_radiologist_index') }}">back to list</a>

    <a href="{{ path('app_radiologist_edit', {'id': radiologist.id}) }}">edit</a>

    {{ include('radiologist/_delete_form.html.twig') }}
{% endblock %}
